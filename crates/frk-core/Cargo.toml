[package]
name = "frk-core"
description = "MLIR context, locations, diagnostics bridge, green tree (SPEC §12)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
melior.workspace = true
