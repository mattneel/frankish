[package]
name = "frk-interp"
description = "Eval interface + derived reference interpreter (SPEC §7.1)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
melior.workspace = true

[dev-dependencies]
frk-core.workspace = true
