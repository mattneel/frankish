# sandbox

xDSL prototypes and one-afternoon spikes (SPEC §11). Nothing here gates CI,
is depended on by `crates/`, or is subject to law L1. Keepers graduate into
the Rust product; the rest is compost.
